# VLog Serena memory policy

This directory must not store point-in-time production status, secrets, personal memory, or duplicated runbooks.

Current architecture and operations live in [the documentation index](../../../docs/README.md). Migration status is tracked in Issue #14. Historical incidents belong under `docs/incidents/` with dates, evidence scope, and an explicit statement that they are not current service status.
