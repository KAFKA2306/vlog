# Packages

Reusable, storage-agnostic capabilities live here. Packages must not import applications or provider-specific adapters.

Materialized packages are `memory-domain/`, `ingestion/`, `companion/`, and `privacy/`. Future package boundaries remain in [Human Memory v2](../docs/architecture/human-memory-v2.md) until implemented.
