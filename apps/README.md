# Applications

Deployable and user-facing entry points live here.

| Boundary | Responsibility |
|---|---|
| `capture-vrchat/` | capture and processing runtime |
| `reader/` | Next.js Reader |
| `companion-lab/` | static public Companion projection |

Only implemented applications are materialized in the repository. Planned API/MCP boundaries remain in [Human Memory v2](../docs/architecture/human-memory-v2.md) until they have executable code.
